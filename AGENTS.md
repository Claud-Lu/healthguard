# HealthGuard 协作规则

## 分支与提交

- `master` 是唯一长期保留的主分支，日常开发、修复和文档维护直接在 `master` 进行。
- 除非用户明确要求，不创建 `dev`、功能分支或额外 worktree；本项目以此规则覆盖通用的新建功能分支习惯。
- 开始工作先检查当前分支和工作区，保留已有未提交改动；不要自动覆盖或丢弃其他人的工作。
- 每次提交前、推送前先拉取并整合 `origin/master` 的最新代码。优先快进，出现分叉时审查双方改动，不强推。
- 完成修改后执行与改动相关的验证，提交并推送 `master`；分支清理前确认所有独有提交已被主分支完整保留。

## 项目文档

- 开发与运维流程见 `docs/ai-cli-operations.md`、`docs/operator-guide.md`；重要变更记录在 `CHANGELOG.md`。
- 开源仓库只保存通用代码和文档，不提交真实部署地址、业务账号、密钥或客户数据。
